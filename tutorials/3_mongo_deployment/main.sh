# Scratch pad when working on this tutorial:
# https://www.linode.com/docs/guides/set-up-mongodb-on-docker/

# sudo docker run --name mongo_example -d mongo:6.0.5
# sudo docker run --name rl-mongo -e MONGO_INITDB_ROOT_USERNAME=$MONGO_USER -e MONGO_INITDB_ROOT_PASSWORD=$MONGO_PASSWORD -p 27017:27017 -d mongo:6.0.5